import { formSlice } from "../slice/slice";

export const { addItemToForm, removeItemFromForm, sortByProp, filterByGender, filterByInterest, returnDataAfterFiltering, sortByPropDesc } = formSlice.actions;
